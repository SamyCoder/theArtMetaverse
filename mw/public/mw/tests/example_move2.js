// This is more of a test than an example.  It shows
// a bad way to do animation.

(function() {

    var pre = mw_getScriptOptions().prefix;

    mw_addActor(pre+'plane.x3d');
    mw_addActor(pre+'move.x3d');
    mw_addActor(pre+'move.x3d');
})();
